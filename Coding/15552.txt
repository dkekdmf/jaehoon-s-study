#include <stdio.h>
#pragma warning(disable:4996)


int main(void) {

	int N, X;
	scanf("%d %d", &X, &X);

	int value;

	for (int i = 0; i < N; i++) {

		if (value < X)
			printf("%d", &value);
	}
	return 0;

	
	
}